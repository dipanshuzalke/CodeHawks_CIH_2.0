export interface RoadmapInput {
  user_name: string;
  user_email: string;
  goal: string;
  skill_level: string;
  months: number;
  daily_hours: number;
  target_companies_or_roles: string | null;
} 